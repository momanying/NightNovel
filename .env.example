# MongoDB 连接配置
MONGODB_URI=mongodb://localhost:27017/nightnovel

# JWT 密钥配置（用于用户认证）
JWT_SECRET=your_jwt_secret_key

# 服务器配置（可选）
PORT=3000
HOST=localhost

# 其他可选配置
NODE_ENV=development
# NODE_ENV=production

# 数据库配置示例（如果使用验证）
# MONGODB_URI=mongodb://username:password@localhost:27017/nightnovel

# 如果使用 MongoDB Atlas，连接字符串示例
# MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/nightnovel?retryWrites=true&w=majority 