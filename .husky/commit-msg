#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

# 检查commitlint是否已安装
if command -v pnpm commitlint >/dev/null 2>&1; then
  pnpm commitlint --edit $1
else
  echo "警告: commitlint未安装或配置，跳过检查"
  exit 0
fi 